import { Scrambler, Seed, Scramble } from '../../models/Scrambler';
export declare class Sq1Scrambler implements Scrambler {
    private sq1Shape;
    private sq1Square;
    private fullCube;
    private search;
    initialize(src: Seed): void;
    setRandomSource(src: Seed): void;
    setScrambleLength(): void;
    getRandomScramble(): Scramble;
}
