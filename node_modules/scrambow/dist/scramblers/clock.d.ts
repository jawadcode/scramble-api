import { Seed, Scrambler } from '../models/Scrambler';
export declare class ClockScrambler implements Scrambler {
    private randomSource;
    initialize(src: Seed): void;
    setRandomSource(src: Seed): void;
    setScrambleLength(): void;
    getRandomScramble(): {
        state: {
            dials: number[];
            pegs: number[];
        };
        scramble_string: string;
    };
}
